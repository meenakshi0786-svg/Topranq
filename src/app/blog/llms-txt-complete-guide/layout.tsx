import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "llms.txt: The Complete Guide to AI-Optimized Site Indexing",
  description: "Everything you need to know about llms.txt — the file that helps AI models understand your website. How to create, score, and deploy it for GEO.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
